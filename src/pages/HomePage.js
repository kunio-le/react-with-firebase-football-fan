import React, { Fragment } from 'react';

import Featured from '../components/featured';

const HomePage = () => {
    return (
        <Fragment>
            <Featured />
        </Fragment>
    );
};

export default HomePage;
